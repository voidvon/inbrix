{
  "name": "lilmail-client-js",
  "version": "0.0.0",
  "private": true,
  "description": "Typecheck/lint config for lilmail's static client JS (assets/js, assets/sw.js). Not a runtime npm project — the app is a single Go binary with no build step.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json && tsc -p assets/tsconfig.json",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@types/serviceworker": "^0.0.200",
    "eslint": "^10.8.0",
    "typescript": "6.0.3",
    "typescript-eslint": "^8.66.0"
  }
}
